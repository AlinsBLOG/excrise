<template>
  <div class="Home">
    <Head-Top head-title='顾问'></Head-Top>
    <div class="search-div">
      <span @click='search'>
        <i class="iconfont icon-sousuo"></i> 搜索顾问
      </span>
    </div>
    <group>
      <x-input title="title"></x-input>
    </group>
    <div class="adviser-box">
      <mt-swipe>
        <mt-swipe-item v-for='(item, index) in bannerList' :key='index'>
          <img :src="item.image">
        </mt-swipe-item>
      </mt-swipe>
    </div>
    <Search-List></Search-List>
    <Home-Footer></Home-Footer>
  </div>
</template>

<script>
  import { mapState,mapMutations,mapGetters  } from 'vuex'
  import HeadTop from '@/components/header/head'
  import HomeFooter from '@/pages/footer'
  import SearchList from '@/components/searchList'
  import { getBanner } from '@/service/getData'

  export default {
    name:"Adviser",
    data(){
      return{
        bannerList: []
      }
    },
    components:{
      HomeFooter,
      HeadTop,
      SearchList
    },
    mounted:function(){
      this.getInfo()
    },
    methods:{
      search () {
        this.$router.push({
          path: 'adviser/search'
        })
      },
      async getInfo () {
        const that = this
        await getBanner('banner').then(res => {
          that.bannerList = res.data.banner
          console.log(res.data.banner)
        })
      }
    }
  }
</script>

<style lang="scss" scoped>
  .search-div {
    background-color: #3190e8;
    padding: .2rem .5rem .3rem;
    span {
      background-color: #fff;
      display: block;
      border-radius: 2px;
      padding: .15rem;
      color: #969696;
      font-size: .4rem;
    }
  }
  .adviser-box {
    height: 4rem;
    img {
      width: 100%;
      height: 100%;
    }
  }

  .module-adviser-list.ceiling {
    margin-top: .2rem !important;
  }
</style>

